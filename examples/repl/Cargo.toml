[package]
name = "lite-agent-repl"
version = "0.1.0"
edition = "2021"

[dependencies]
lite-agent = { path = "../.." }
serde_json = "1"
tokio = { version = "1", features = ["macros", "process", "rt-multi-thread", "io-std", "io-util", "time"] }
