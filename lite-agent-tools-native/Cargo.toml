[package]
name = "lite-agent-tools-native"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
thiserror = "2"
tokio = { version = "1", features = ["io-util", "macros", "process", "rt", "time"] }

[dev-dependencies]
tempfile = "3"
